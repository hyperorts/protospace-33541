<main class="main">
  <div class="inner">
    <% if user_signed_in? %>
      <div class="greeting">
      こんにちは、
      <%= link_to "#{current_user.name}さん", user_path(current_user.id), class: :greeting__link%> <%#　←ログインしているユーザなのでこの組み合わせ %>
      </div>
    <% end %>
    <div class="card__wrapper">
      <%# ↓投稿機能実装後、部分テンプレートでプロトタイプ投稿一覧を表示する %>
      <%= render partial:"prototype", collection: @prototypes  %>
    </div>
  </div>
</main>